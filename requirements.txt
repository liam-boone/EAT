# Build steps 1-6: section engine, material list, beam engine, DXF I/O,
# FastAPI layer, frontend (plain HTML/CSS/JS, served as static files by
# the same backend -- no build step, no extra dependency here).
sectionproperties>=3.10,<4
numpy>=1.26
ezdxf>=1.4,<2
fastapi>=0.115,<1
uvicorn>=0.30,<1
python-multipart>=0.0.9,<1

# Only needed to run eat/verify_api.py (FastAPI's TestClient).
httpx>=0.27,<1

# Only needed to run eat/verify_frontend.py (headless-browser UI check).
# After installing, also run once: playwright install chromium
playwright>=1.40,<2
