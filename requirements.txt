# Build steps 1-3: section engine, material list, beam engine.
# Later build steps (DXF, API, frontend) will add ezdxf, fastapi, uvicorn,
# etc. here as they land.
sectionproperties>=3.10,<4
numpy>=1.26
