# Build steps 1-5: section engine, material list, beam engine, DXF I/O,
# FastAPI layer. The frontend (last step) needs nothing further here —
# it's served by this same backend.
sectionproperties>=3.10,<4
numpy>=1.26
ezdxf>=1.4,<2
fastapi>=0.115,<1
uvicorn>=0.30,<1
python-multipart>=0.0.9,<1

# Only needed to run eat/verify_api.py (FastAPI's TestClient).
httpx>=0.27,<1
